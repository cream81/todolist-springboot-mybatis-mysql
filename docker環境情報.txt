openJDK11
→winpty docker exec -it test_java3 bash
FROM mysql:8.0.16
→winpty docker exec -it test_db3 bash
データベースを削除する場合下記フォルダを削除して
docker-compose downを実施
.\mysql\db

※spring bootプロジェクトはeclipseか
https://start.spring.io/から作成する。

export NODE_OPTIONS=--openssl-legacy-provider
なお、この問題は webpack の v6 で修正されるようです。




